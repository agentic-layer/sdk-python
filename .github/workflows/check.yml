name: Check
on:
  push:
    branches:
      - main
      - 'renovate/**'
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: 'actions/checkout@v7'

      - name: Install uv
        uses: 'astral-sh/setup-uv@v8.3.2'
        with:
          version: "0.11.29"
          enable-cache: true

      - name: Run checks
        run: make check
